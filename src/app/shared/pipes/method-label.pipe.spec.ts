import { MethodLabelPipe } from './method-label.pipe';

describe('MethodLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new MethodLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
